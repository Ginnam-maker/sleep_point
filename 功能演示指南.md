# 🎉 功能演示指南

## 快速体验完整功能流程

### 第1步：首次打卡 🎊

1. **打开首页**
   - 查看根据时间显示的问候语
   - 阅读随机出现的助眠语录
   
2. **选择心情并打卡**
   - 点击任意一个心情按钮（如 😊 高兴）
   - 看到"打卡成功！"提示
   - 🎉 弹窗显示"解锁新成就：初来乍到"
   
3. **查看打卡状态**
   - 页面自动更新为"今日已打卡"状态
   - 显示打卡时间和选择的心情

### 第2步：查看统计页 📊

1. **点击底部"统计"按钮**
   
2. **查看核心指标**
   - 连续打卡：1天（黑色卡片，有🔥图标）
   - 累计天数：1天
   
3. **查看日历**
   - 今日显示黑色边框高亮
   - 今日格子内显示您选择的心情表情
   
4. **查看心情分布**
   - 显示您刚才选择的心情
   - 彩色进度条显示100%（因为只有1条记录）
   - 显示"1次"

### 第3步：查看成就 🏆

1. **点击底部"成就"按钮**
   
2. **查看解锁进度**
   - 顶部显示：1 / 9 （已解锁1个，共9个）
   
3. **找到已解锁的成就**
   - "🎉 初来乍到"显示为黑色卡片
   - 金色圆形图标背景
   - 右下角绿色勾选标记
   - 底部显示解锁日期
   
4. **查看未解锁的成就**
   - 其他8个成就显示为灰色半透明
   - 底部显示解锁条件
   
5. **点击任意成就**
   - 弹出详情窗口
   - 查看成就名称、描述、解锁条件
   - 已解锁的显示具体解锁时间

### 第4步：连续打卡测试 🔥

> 注意：以下需要修改系统日期或等待真实时间

1. **第2天打卡**
   - 返回首页
   - 选择另一个心情（如 😌 满足）
   - 打卡成功
   - 查看统计页：连续打卡变为2天
   
2. **第3天打卡**
   - 继续打卡
   - 🎉 解锁"🔥 连续坚持者"成就
   - 查看统计页：连续打卡变为3天
   - 成就页显示：2 / 9

3. **测试心情收集**
   - 在不同天使用不同心情
   - 当使用满6种心情时
   - 🎉 解锁"🎭 心情记录官"成就

### 第5步：查看数据变化 📈

1. **统计页日历**
   - 每天打卡后，日历上会出现对应的心情表情
   - 观察本月打卡情况一目了然
   
2. **心情分布变化**
   - 随着打卡天数增加
   - 心情分布的进度条会变化
   - 显示不同心情的使用占比

3. **连续打卡增长**
   - 每天连续打卡，连续天数+1
   - 如果某天漏打卡，连续天数重置为0
   - 但累计天数不受影响

## 🎯 完整体验路径建议

### 第1天：初体验
```
打开应用 → 阅读问候语 → 选择心情 → 打卡成功 
→ 解锁"初来乍到" → 查看统计页 → 查看成就页 
→ 了解其他成就条件
```

### 第2-3天：养成习惯
```
每天睡前打卡 → 选择真实心情 → 第3天解锁"连续坚持者" 
→ 观察统计数据变化 → 尝试不同心情
```

### 第4-7天：冲击目标
```
保持连续打卡 → 第7天解锁"习惯养成者" 
→ 如果集齐6种心情，解锁"心情记录官"
→ 尝试23:00前打卡（为"早睡先锋"做准备）
```

### 长期使用
```
连续30天 → "睡眠守护者"
当月全勤 → "全勤之星"
累计100天 → "持之以恒"
```

## 🐛 测试技巧

### 快速测试成就解锁（开发模式）

在微信开发者工具的控制台执行：

```javascript
// 1. 清除所有数据（重新开始）
wx.clearStorageSync();
console.log('数据已清除');

// 2. 查看今日打卡记录
const today = new Date().toISOString().split('T')[0];
console.log('今日日期:', today);
console.log('今日打卡:', wx.getStorageSync('checkin_' + today));

// 3. 查看所有打卡记录
const info = wx.getStorageInfoSync();
const checkins = [];
info.keys.forEach(key => {
    if (key.startsWith('checkin_')) {
        checkins.push(JSON.parse(wx.getStorageSync(key)));
    }
});
console.log('所有打卡记录:', checkins);

// 4. 查看已解锁成就
const achievements = wx.getStorageSync('achievements');
console.log('已解锁成就:', achievements ? JSON.parse(achievements) : []);

// 5. 模拟多天打卡（测试用）
// 注意：这只是测试，实际应用中每天只能打卡一次
const moods = ['happy', 'content', 'sad', 'tired', 'angry', 'worried'];
for (let i = 0; i < 7; i++) {
    const date = new Date();
    date.setDate(date.getDate() - i);
    const dateStr = date.toISOString().split('T')[0];
    const record = {
        mood: moods[i % 6],
        time: date.getTime(),
        date: dateStr
    };
    wx.setStorageSync('checkin_' + dateStr, JSON.stringify(record));
}
console.log('已模拟7天打卡记录');
```

## 📱 真机测试流程

1. **在微信开发者工具中点击"预览"**
2. **用微信扫描二维码**
3. **在手机上测试所有功能**
4. **注意事项**：
   - 真机上的触感和动画效果更好
   - 可以真实体验每天打卡的场景
   - 建议每天固定时间使用

## 🎨 视觉效果检查清单

### 首页
- [ ] 问候语显示正常
- [ ] 助眠语录每次打开都不同
- [ ] 未打卡时显示提示文字
- [ ] 已打卡时显示时间和心情
- [ ] 心情按钮布局整齐（3×2网格）
- [ ] 点击后有视觉反馈

### 统计页
- [ ] 连续打卡天数在黑色卡片中
- [ ] 日历显示完整（周日到周六）
- [ ] 今日有边框高亮
- [ ] 打卡日显示心情表情
- [ ] 心情分布进度条有颜色
- [ ] 数据切换后自动更新

### 成就页
- [ ] 已解锁成就是黑色卡片
- [ ] 未解锁成就是半透明灰色
- [ ] 已解锁显示日期
- [ ] 未解锁显示条件
- [ ] 点击弹窗显示正常
- [ ] 弹窗可以关闭

## ✨ 最佳演示脚本

**适用场景**：向他人展示这个小程序

1. **开场**（10秒）
   - "这是一个极简的睡眠打卡小程序"
   - 展示首页的简洁设计

2. **核心功能**（30秒）
   - "每天选择心情打卡"
   - 演示点击心情按钮
   - 展示成就解锁提示

3. **数据统计**（20秒）
   - 切换到统计页
   - "可以看到连续打卡天数"
   - "日历显示本月所有打卡"
   - "心情分布一目了然"

4. **成就系统**（20秒）
   - 切换到成就页
   - "9种成就等你解锁"
   - 点击查看成就详情
   - "激励自己养成良好作息"

5. **总结**（10秒）
   - "极简设计，专注睡眠打卡"
   - "通过成就激励，养成好习惯"

---

**准备时间**：5分钟  
**演示时长**：1-2分钟  
**建议**：提前打卡几天，有数据更好展示 📊✨
